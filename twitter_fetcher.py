"""\ntwitter_fetcher.py\nTwitter API v2 트렌드/트윗 수집\n\n[기존 기능] fetch_trends()        — 타임라인 Top 3 + 대한민국 트렌딩 Top 3\n[신규 기능] fetch_tweet_context() — X URL → 원본 + 인용RT + 관련 트윗 수집\n\n필요: TWITTER_BEARER_TOKEN (읽기 전용, 무료)\n"""\nimport os\nimport re\n\nimport requests\n\nKR_WOEID = 23424868\n\n\ndef _headers() -> dict:\n    token = os.environ.get(\"TWITTER_BEARER_TOKEN\", \"\")\n    if not token:\n        raise ValueError(\"TWITTER_BEARER_TOKEN 환경변수가 없습니다.\")\n    return {\"Authorization\": f\"Bearer {token}\"}\n\n\n# ── 기존: 트렌드 수집 ────────────────────────────────────────\n\ndef fetch_trends() -> dict:\n    h = _headers()\n    print(\"  → 타임라인 분석 중...\")\n    timeline = _fetch_timeline_trends(h)\n    print(\"  → 대한민국 트렌딩 수집 중...\")\n    trending = _fetch_kr_trending(h)\n    return {\"timeline\": timeline, \"trending\": trending}\n\n\ndef _fetch_timeline_trends(headers):\n    try:\n        me = requests.get(\"https://api.twitter.com/2/users/me\", headers=headers, timeout=10)\n        me.raise_for_status()\n        user_id = me.json()[\"data\"][\"id\"]\n        tl = requests.get(\n            f\"https://api.twitter.com/2/users/{user_id}/timelines/reverse_chronological\",\n            headers=headers,\n            params={\"max_results\": 100, \"tweet.fields\": \"public_metrics,entities\"},\n            timeout=15,\n        )\n        tl.raise_for_status()\n        return _extract_topics_from_tweets(tl.json().get(\"data\", []))\n    except Exception as e:\n        print(f\"  ⚠️  타임라인 수집 실패: {e}\")\n        return []\n\n\ndef _extract_topics_from_tweets(tweets):\n    topic_stats = {}\n    for tweet in tweets:\n        metrics  = tweet.get(\"public_metrics\", {})\n        rt_count = metrics.get(\"retweet_count\", 0)\n        score    = rt_count * 3 + metrics.get(\"like_count\", 0)\n        tags = [h[\"tag\"] for h in tweet.get(\"entities\", {}).get(\"hashtags\", [])]\n        if not tags:\n            tags = _extract_keywords(tweet.get(\"text\", \"\"))\n        for tag in tags:\n            tag = tag.strip(\"#\").strip()\n            if len(tag) < 2: continue\n            if tag not in topic_stats:\n                topic_stats[tag] = {\"rt_count\": 0, \"score\": 0, \"tweet_count\": 0}\n            topic_stats[tag][\"rt_count\"]    += rt_count\n            topic_stats[tag][\"score\"]       += score\n            topic_stats[tag][\"tweet_count\"] += 1\n    sorted_topics = sorted(topic_stats.items(), key=lambda x: x[1][\"score\"], reverse=True)[:3]\n    return [{\"rank\": i+1, \"topic\": t, \"rt_count\": s[\"rt_count\"], \"tweet_count\": s[\"tweet_count\"]}\n            for i, (t, s) in enumerate(sorted_topics)]\n\n\ndef _fetch_kr_trending(headers):\n    try:\n        resp = requests.get(\n            \"https://api.twitter.com/1.1/trends/place.json\",\n            headers=headers, params={\"id\": KR_WOEID}, timeout=10,\n        )\n        resp.raise_for_status()\n        trends = resp.json()[0].get(\"trends\", [])\n        valid  = [t for t in trends if t.get(\"tweet_volume\")] or trends\n        return [{\"rank\": i+1, \"topic\": t[\"name\"].lstrip(\"#\"), \"tweet_volume\": t.get(\"tweet_volume\", 0)}\n                for i, t in enumerate(valid[:3])]\n    except Exception as e:\n        print(f\"  ⚠️  트렌딩 수집 실패: {e}\")\n        return []\n\n\n# ── 신규: URL로 트윗 컨텍스트 수집 ──────────────────────────\n\ndef fetch_tweet_context(url: str) -> dict:\n    \"\"\"X URL → 원본 + 인용RT + 관련 트윗 종합 수집.\"\"\"\n    h        = _headers()\n    tweet_id = _extract_tweet_id(url)\n    if not tweet_id:\n        raise ValueError(f\"트윗 ID를 추출할 수 없습니다: {url}\")\n    print(f\"  → 원본 트윗 조회 중... (ID: {tweet_id})\")\n    original = _fetch_single_tweet(tweet_id, h)\n    print(\"  → 인용 RT 수집 중...\")\n    quotes   = _fetch_quote_tweets(tweet_id, h)\n    print(\"  → 관련 트윗 검색 중...\")\n    keywords = _extract_keywords(original.get(\"text\", \"\"))\n    related  = _fetch_related_tweets(keywords, tweet_id, h)\n    summary  = _build_summary(original, quotes, related, keywords)\n    print(f\"  → 수집 완료: 원본 1 + 인용RT {len(quotes)} + 관련 {len(related)}개\")\n    return {\"original\": original, \"quote_tweets\": quotes, \"related\": related, \"summary\": summary}\n\n\ndef _extract_tweet_id(url):\n    m = re.search(r\"status/(\\d+)\", url)\n    return m.group(1) if m else None\n\n\ndef _fetch_single_tweet(tweet_id, headers):\n    try:\n        resp = requests.get(\n            f\"https://api.twitter.com/2/tweets/{tweet_id}\",\n            headers=headers,\n            params={\"tweet.fields\": \"public_metrics,entities,author_id,created_at\",\n                    \"expansions\": \"author_id\", \"user.fields\": \"name,username\"},\n            timeout=10,\n        )\n        resp.raise_for_status()\n        data   = resp.json()\n        tweet  = data.get(\"data\", {})\n        users  = {u[\"id\"]: u for u in data.get(\"includes\", {}).get(\"users\", [])}\n        author = users.get(tweet.get(\"author_id\", \"\"), {})\n        return {\"id\": tweet.get(\"id\",\"\"), \"text\": tweet.get(\"text\",\"\"),\n                \"author\": author.get(\"name\",\"\"), \"username\": author.get(\"username\",\"\"),\n                \"metrics\": tweet.get(\"public_metrics\",{}), \"created_at\": tweet.get(\"created_at\",\"\")}\n    except Exception as e:\n        print(f\"  ⚠️  원본 트윗 조회 실패: {e}\")\n        return {}\n\n\ndef _fetch_quote_tweets(tweet_id, headers, max_results=10):\n    try:\n        resp = requests.get(\n            f\"https://api.twitter.com/2/tweets/{tweet_id}/quote_tweets\",\n            headers=headers,\n            params={\"max_results\": max_results, \"tweet.fields\": \"public_metrics\"},\n            timeout=10,\n        )\n        resp.raise_for_status()\n        return [{\"text\": t.get(\"text\",\"\"), \"metrics\": t.get(\"public_metrics\",{})}\n                for t in resp.json().get(\"data\", [])]\n    except Exception as e:\n        print(f\"  ⚠️  인용RT 수집 실패: {e}\")\n        return []\n\n\ndef _fetch_related_tweets(keywords, exclude_id, headers, max_results=10):\n    if not keywords: return []\n    query = f\"({' OR '.join(keywords[:3])}) -is:retweet -is:reply lang:ko\"\n    try:\n        resp = requests.get(\n            \"https://api.twitter.com/2/tweets/search/recent\",\n            headers=headers,\n            params={\"query\": query, \"max_results\": max_results, \"tweet.fields\": \"public_metrics\"},\n            timeout=10,\n        )\n        resp.raise_for_status()\n        return [{\"text\": t.get(\"text\",\"\"), \"metrics\": t.get(\"public_metrics\",{})}\n                for t in resp.json().get(\"data\", []) if t.get(\"id\") != exclude_id]\n    except Exception as e:\n        print(f\"  ⚠️  관련 트윗 검색 실패: {e}\")\n        return []\n\n\ndef _build_summary(original, quotes, related, keywords):\n    all_metrics = [original.get(\"metrics\",{})] + [t[\"metrics\"] for t in quotes + related]\n    rt_counts   = [m.get(\"retweet_count\", 0) for m in all_metrics if m]\n    like_counts = [m.get(\"like_count\", 0) for m in all_metrics if m]\n    avg_rt      = int(sum(rt_counts) / len(rt_counts)) if rt_counts else 0\n    sentiment   = \"긍정 반응 다수\" if sum(like_counts) > avg_rt * 5 else \"반응 혼재\"\n    combined    = f\"[원본]\\n{original.get('text','')}\\n\\n\"\n    if quotes:\n        combined += \"[인용 반응]\\n\" + \"\\n\".join(f\"- {t['text']}\" for t in quotes[:5]) + \"\\n\\n\"\n    if related:\n        combined += \"[관련 트윗]\\n\" + \"\\n\".join(f\"- {t['text']}\" for t in related[:5])\n    return {\"topic\": \" \".join(keywords[:3]), \"total_collected\": 1+len(quotes)+len(related),\n            \"avg_rt\": avg_rt, \"sentiment_hint\": sentiment, \"combined_text\": combined.strip()}\n\n\ndef _extract_keywords(text):\n    text = re.sub(r\"https?://\\S+\", \"\", text)\n    text = re.sub(r\"@\\w+\", \"\", text)\n    text = re.sub(r\"RT\\s+\", \"\", text)\n    words = re.findall(r\"[가-힣A-Za-z]{2,}\", text)\n    stopwords = {\"RT\",\"the\",\"and\",\"for\",\"that\",\"this\",\"있다\",\"없다\",\"한다\",\"됩니다\",\"합니다\",\"입니다\",\"것이\",\"이번\",\"오늘\",\"지금\"}\n    return [w for w in words if w not in stopwords][:3]\n